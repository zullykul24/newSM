<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@drawable/logosmred"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logosmred"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewPaymentActivity"></activity>
        <activity android:name=".ViewVoucherActivity" />
        <activity android:name=".DatePickerActivity" />
        <activity android:name=".AddVoucherActivity" />
        <activity android:name=".ActivityNewFood" />
        <activity
            android:name=".ChooseFoodActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".OrderActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".AddFood" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".PayBillActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>